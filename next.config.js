/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["ecommerce-rigelec.s3.amazonaws.com"],
    },
};

module.exports = nextConfig;
